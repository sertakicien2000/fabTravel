{
  "name": "mern-server",
  "version": "1.0.0",
  "description": "Mern Demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "cd client && npm run start",
    "dev": "concurrently \"nodemon server.js\" \"npm run client\"",
    "setup": "npm install && cd client && npm install",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.5.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mongoose": "^5.0.16",
    "multer": "^1.3.0",
    "react-share": "^3.0.1",
    "react-social-sharing": "file:react-social-sharing",
    "uuid": "^3.2.1"
  }
}
